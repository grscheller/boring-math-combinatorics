[build-system]
requires = ["flit_core>=3.12,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "boring-math-combinatorics"
version = "2.0.1"
readme = "README.rst"
requires-python = ">=3.13"
authors = [{ name = "Geoffrey R. Scheller", email = "geoffrey@scheller.com" }]
keywords = [
  "combinations",
  "permutations",
  "combinatorics",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Framework :: Pytest",
  "Intended Audience :: Education",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = [
  "pythonic-fp-circulararray>=6.0.0",
  "pythonic-fp-fptools>=5.1.2",
  "pythonic-fp-iterables>=5.1.2",
]
description = "Combinatorics library writtten in pure Python"

[project.urls]
Changelog = "https://github.com/grscheller/boring-math-combinatorics/blob/main/CHANGELOG.md"
Documentation = "https://grscheller.github.io/boring-math/homepage/build/html/projects/combinatorics.html"
Homepage = "https://grscheller.github.io/boring-math/"
Source = "https://github.com/grscheller/boring-math-combinatorics"

[project.optional-dependencies]
test = [
  "pytest>=8.4.1",
]

[tool.flit.module]
name = "boring_math.combinatorics"

[tool.mypy]
explicit_package_bases = true
local_partial_types = true
namespace_packages = true
warn_redundant_casts = true
warn_return_any = true
warn_unused_configs = true

[tool.pylsp-mypy]
enabled = true
live-mode = true
dmypy = false
strict = true
report_progress = true

[tool.pytest.ini_options]
addopts = "-ra"
consider_namespace_packages = true
testpaths = ["tests/"]

[tool.ruff]
target-version = "py313"

[tool.ruff.lint.flake8-quotes]
docstring-quotes = "double"

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = [
  "E731",
  "C901",
]
"**/*.py" = ["E741"]

[tool.ruff.format]
quote-style = "single"
docstring-code-line-length = 72
